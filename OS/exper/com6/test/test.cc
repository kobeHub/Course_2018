#include <stdio.h>
#include <stdlib.h>

int main()
{
    int te[5];
    enum dir{le, ti};
    te[1] = ti;
    dir m;
    m = le;
    printf("%d%d\n",te[1],dir(m));

	int a[50] = {0};
	for(int i=0; i<5; i++)
	  printf('%d', a[i]);
}

